name: Rebase open PRs

on:
  push:
    branches:
      - develop

jobs:
  rebase-prs:
    uses: mPokornyETM/rebase-open-prs-action/.github/workflows/rebase-open-prs.yml@v1
    secrets:
      GH_PAT: ${{ github.token }}